#pragma once

// Add includes and custom types here that are used in dzn models and thus needed in generated source code.

#pragma warning(disable:4512) // For dezyne code disable warning "Assignment operator cannot be generated"
#pragma warning(disable:4100) // For dezyne code disable warning "'identifier' : unreferenced formal parameter"
#pragma warning(disable:4189) // For dezyne code disable warning "'identifier' : local variable is initialized but not referenced"

#include <string>
#include "calling_context.hh"

